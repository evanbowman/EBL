(require "lisp/stdstr.ebl")
(require "lisp/stdfs.ebl")

(namespace csv
  (defn read (file-name)
    (fs::map-lines file-name
                   (lambda (line)
                     (std::split line \,)))))
