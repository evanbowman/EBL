(require "ebl/stdfs.ebl")
(require "ebl/stdstr.ebl")

(namespace csv
  (defn read (file-name)
    (fs::map-lines file-name
                   (lambda (line)
                     (std::split line \,)))))
